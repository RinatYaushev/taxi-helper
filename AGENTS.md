# AGENTS.md

Контекст і правила для AI-агентів (Copilot тощо), що працюють у цьому репозиторії.

## Про проєкт

`taxi-helper` — персональний калькулятор прибутковості поїздок у таксі **Uklon**.
Мета — максимізувати чистий прибуток через відсів невигідних замовлень і власні
фільтри в «Автопілоті».

### Де ми зараз (стан на 29.08.2026)

База — **164 поїздки** за серпень (15 змін), `data.offers` і `data.idles` порожні,
`pickup_km` — 0 із 164. Модель часу відкалібрована (`10.19 + 1.96×км`), єдина
шкала рішень — ₴/год із ціллю **240**.
Слоти дають **287 ₴/год** проти бази 267 (141 із 164 проходять).

**Що змінилось в останній сесії** (рефакторинг, ще не закомічений).
Спершу — **чому база 276 → 267 ₴/год**, розкладено по внесках:

| крок | ₴/год |
|---|---|
| попередній стан | 276.0 |
| + комісія за вивід із «Комбінованої» | 275.0 (−1.0) |
| + рекалібрована `cycle_model` після фіксу дат | 267.8 (**−7.2**) |
| + виправлена зона однієї поїздки (Стрижавка) | **267.0** (−0.8) |

Тобто майже вся зміна — **один** ефект: ISO-дати полагодили межі змін, регресія
перерахувалась (`10.6+1.79` → `10.19+1.96`), і година замовлення подорожчала.
Решта — шум. Перелік змін:

- **`datetime` → ISO** `YYYY-MM-DD HH:MM` (`npm run migrate --write`): без року
  серпень різних років зливався в один день, а зміни різались опівночі. Це
  **головна** змістовна правка — від неї поїхали всі часові числа;
- **комісія за вивід** береться лише з безготівкової частини «Комбінованої»
  (`amount_balance` / `combined_balance_share`). Напрямок правильний, але ціна
  питання мала: **54 ₴ із 15 923 (0.34%)**, і `amount_balance` заповнений у
  **0 із 164** поїздок — поки це здогадка `0.5`, а не вимір;
- мертві `settings.modes` і `settings.time_model` **видалені** — 4 «режими»
  бектестились правилом, якого у формі Uklon немає;
- `generate.ts` (1600 рядків) розібраний на `src/report/**`, зʼявились
  `time.ts` / `validate.ts` / `offers.ts`, 23 юніт-тести і `npm run ci`;
- 🔴 **фікс: слот більше не змішує вкладки «Простий» і «Складний»** — раніше звіт
  міг надрукувати інструкцію, яку неможливо ввести у форму (див. розділ про слоти);
- ⚠️ **утилізація 100% більше не є доказом** якості моделі — вона тавтологічна;
- **бутстреп прибрано з ВИБОРУ слотів** (лишився як діагностика) — виміряно, що
  він шкодив: див. «Аудит методики»;
- **валідатор**: злиплі адреси тепер оцінюються за впливом на зону, а не просто
  перелічуються; додано перевірку «довга поїздка з зоною Місто».

> ⚠️ **Чого в цій сесії НЕ було, попри попередню редакцію цього файлу.**
> Зональний порожняк (`empty_run_by_zone`) уже існував і вже застосовувався в
> `compute` — змінилось лише **золоте правило** (`minGrossPerKm` →
> `minGrossPerKmZone`), а воно тільки показове й на рішення не впливає.
> Тупики **не** падали з 206 до 193: за старим і новим циклом однаково **187**
> ₴/год. Поріг передмістя рухається від `Км у мінімалці` (`ceil(B + A/K)`), а не
> від порожняку. Не переносити ці твердження далі.

Три відкриті питання, у порядку важливості:

1. 🔴 **Правило проходження фільтра не встановлене** — офіційний опис суперечить
   сам собі (адитивна vs мультиплікативна модель). Ціна помилки — тупики, що
   просочуються крізь ціновий поріг, і потреба у whitelist секторів.
   Тепер це рахує **`npm run offers`** (`filterRuleEvidence`) — потрібні лише
   записи в `data.offers`.
2. 🟠 **`pickup_km` не збирається** — радіус подачі лишається єдиним полем, яке
   виводиться з теорії, а не з фактів. Водій погодився записувати; треба 20–30 замірів.
3. 🟠 **Ціна відмови невідома** — скільки простою купує вузький фільтр. Це не
   абстракція: вибір між строгим і мʼяким набором слотів **перевертається** на
   заповненні 74% (див. «Аудит методики»). Закривається журналом `data.offers`
   (відмови) + `data.idles` (тиша).

> Усі три закриваються **одним** джерелом — журналом пропозицій `data.offers`.
> Це єдині дані, яких у нас принципово немає: `trips` — цензурована вибірка
> (лише те, що водій уже взяв). Просити водія вести саме цей журнал.

Чого **не** робити (перевірено, закрито): не збирати зворотний пробіг з тупиків,
не додавати надбавку як поле фільтра, не планувати графік за годиною виїзду —
деталі у «Доменних висновках».


### Профіль водія (важливо для розрахунків)
- Місто: **Вінниця** (компактне, короткі плечі, є «глухі кути» — навколишні села).
- Авто: **Ford Fusion 2.5** (атмосферник) з **ГБО** → дешевий кілометр на газі.
- Графік: вечори буднів + вихідні (піковий/вечірній попит).
- Витрата газу: ~13–14 л/100 км. Ціна газу змінна (задається в `settings`).

## Архітектура даних

Єдине джерело правди — **`data.json`** (`settings` + масив `trips`).
Усе інше — генератори/аналізатори поверх нього. HTML-звіт — похідний артефакт (в git не коммітиться).

Схема поїздки:
```json
{ "datetime": "16.08 19:53", "payment": "Безготівка", "amount": 185,
  "distance": 6.23, "pickup_km": 1.2, "pickup_min": 7,
  "from": "...", "to": "...", "zone": "Місто" }
```
- `payment`: `Готівка` | `Безготівка` | `Комбінована`
- `zone`: `Місто` | `Глухий кут` — визначається **за адресою призначення** (`to`).
- `pickup_km`, `pickup_min` *(опційно)*: відстань і час подачі до клієнта.
  Старі поїздки без них; перевірка подачі в бектесті вмикається автоматично,
  щойно `pickup_km` зʼявиться. **Зараз заповнені в 0 із 164** — див. розділ «Подача».
- `data.idles` *(опційно)* — ручний журнал простоїв (`types.ts::Idle`).
  **Зараз порожній.** Навіщо — див. розділ «Простої».

## Ключові команди

```bash
npm install             # залежності (jimp)
npm run generate        # data.json -> report.html (самодостатній HTML-звіт)
npm run analyze         # per-trip грн/км, чист/км
npm run report          # зведення по дистанції/зоні/годині/оплаті
npm run calibrate       # перекалібрування cycle_model на інтервалах між поїздками
npm run parse           # /tmp/ocr.txt -> нові поїздки в data.json (--write щоб записати)
npm run update-pay      # оновлення payment за кольором іконки
./pipeline.sh [dir]     # повний конвеєр обробки скрінів (default ~/Desktop/screens)
```

> Стек: **TypeScript на Node.js 22+** (запуск напряму, без збірки — Node сам стрипає
> типи). Пиши **erasable** TS: без `enum`/`namespace`/parameter-properties.
> Локальні імпорти — з розширенням `.ts` (`import ... from "./lib.ts"`).

## Обробка скріншотів замовлень (macOS)

1. `swift ocr.swift <dir>` → текст (адреси, `сума ₴`, `відстань км`, `DD серп. HH:MM`).
2. `swift ocr_boxes.swift <dir>` → ті ж рядки з нормалізованими bounding-box (origin — низ-ліво).
3. `npm run update-pay` (`src/updatePay.ts`) → визначає **тип оплати за кольором іконки**
   ліворуч від суми:
   - 🟡 жовтий (H≈38–62°) → **Готівка**
   - 🟢 зелений (H≈90–165°) → **Комбінована** (гаманець: частина готівка + частина на баланс)
   - 🔵 синій (H≈190–255°) → **Безготівка**
   Іконка — суцільне кольорове коло, ~1500–2000 насичених пікселів; сигнал чистий і однозначний.

### Як додавати НОВІ поїздки зі скрінів
Автоматично через `src/parseOrders.ts` (`npm run parse`): читає `/tmp/ocr.txt`,
витягує `datetime/amount/distance/from/to` за якорями (дата, `сума ₴`, `відстань км`),
зливає перенесені адреси, визначає `zone` через `inArea(to, dead_end_areas)`, дедупить
за `datetime`. Без прапорця — dry-run; `--write` дописує в `trips`. Тип оплати потім
проставляє `src/updatePay.ts` за кольором іконки.
> Стійкість OCR: сума нормалізується проти шуму іконки оплати зліва (напр. `9111`→`111`,
> бо міські суми — 2–3 цифри, <1200 грн); `км` матчиться без регістру; сума ловиться навіть
> коли OCR розбив `210 ₴` на `210` + `) ₴`. Проблемні блоки парсер друкує окремо — перевіряй вручну.
> **Звірка суми двома джерелами:** верхнє число біля `₴` та рядок розбивки «Оплата замовлення»
> (`66 ₴ готівкою, 45 ₴ на баланс`) узгоджуються між собою — кожне має свій режим відмови
> (верх: шум іконки; розбивка: OCR обрізає частину, напр. `260 готівкою` без `35 на баланс`).
> Якщо збігаються — тип оплати береться з розбивки (готівка/баланс/обидва → Готівка/Безготівка/Комбінована);
> якщо ні, а верх «чистий» (40–1200) — віримо верху; якщо верх зашумлений (>1200) — розбивці.
> `update-pay` за кольором іконки лишається фінальним джерелом типу оплати.

### Де лежать скріни (постійна конвенція)
Скріни замовлень — `~/Desktop/screens/orders/<DDMMYY>/` (напр. `030926` = 03.09.26).
**Кожна папка — окрема дата**, так буде і надалі. `ocr.swift`/`ocr_boxes.swift`
**не рекурсивні** — читають файли з переданої папки, а не з підпапок. Тому
кожну дату OCR-иш окремо:
```bash
swift ocr.swift ~/Desktop/screens/orders/030926 > /tmp/ocr.txt
node src/parseOrders.ts --write
swift ocr_boxes.swift ~/Desktop/screens/orders/030926 > /tmp/boxes.tsv
node src/updatePay.ts ~/Desktop/screens/orders/030926
```
`update-pay` матчить скрін↔поїздку за відбитком «сума+км» серед **усіх**
поїздок у `data.json` (не лише цієї дати) — це нормально, конфліктів не буває,
бо пара майже унікальна.

### Ручний журнал подачі (паралельно зі скрінами, постійна практика)
Водій веде текстову нотатку (Notes.app) поряд зі скрінами, за зміну, у форматі:
```
HH:MM - km min[ хв][ - взято вручну|взято ланцюгом]
```
- `HH:MM` — момент **прийняття** замовлення (коли прийшла пропозиція). Це НЕ те
  саме, що час `Архівне DD міс. HH:MM` на скріні деталей — той на кілька
  хвилин пізніший (час іде на подачу/обробку). Матчинг рядків нотатки з
  розпізнаними поїздками — **за хронологічним порядком у межах зміни**:
  кількість рядків нотатки завжди дорівнює кількості поїздок за той день;
  зсув між сусідніми парами додатний і зростає монотонно.
- `km` = `pickup_km`, `min` = `pickup_min` — те, що Uklon показує в пропозиції
  (відстань і час підльоту до клієнта), а НЕ параметри самої поїздки.
- **`0 0` — легітимне значення, не пропуск запису.** Означає: наступне
  замовлення забирало з тієї самої адреси, куди щойно привезли попереднього
  пасажира → нульова подача.
- Позначки в кінці рядка — **як узято замовлення в обхід наших фільтрів**,
  мапляться на `Trip.source`:
  - «взято вручну» → `source: "Ефір"` — водій сам переключився на пошук зі
    стрічки (типово: глухий кут, фільтр нічого не знайшов).
  - «взято ланцюгом» → `source: "Ланцюг"` — водій увімкнув режим Цикл без
    обмежень фільтра, щоб узяти БУДЬ-ЯКЕ замовлення й вибратись із глухого
    кута. Без позначки — стандартне взяття через слот Автопілота.

## Формули (єдине джерело — `src/lib.ts::compute`)
```
fuelPerKm  = gas_consumption_l_100km/100 * gas_price_per_l
gas        = distance * (1 + empty_run_coef) * fuelPerKm
commission = amount * commission_uklon_pct/100  (+ commission_cashless_pct% якщо Безготівка)
net        = amount - commission - gas
netPerKm   = net / distance
timeMin    = cycle_model.base_min + per_km_min * distance      ← ВИМІРЯНА
netPerHour = net / (timeMin/60)
```
**Єдина шкала рішень — `netPerHour`** (`lib.ts::compute`):
`netPerHour ≥ target_net_per_hour` → **бери**; `≥ marginalTargetPh` → **думай**;
інакше **пропускай**. `marginalTargetPh = target × (marginal_net_per_km /
threshold_net_per_km)` — зберігає стару ширину смуги «думай» (10/14 → 171 ₴/год),
але міряє її часом.

> ⚠️ Раніше `rec`/`rating`/`badPct` рахувались за **₴/км**, а слоти й режими — за
> **₴/год**. На 164 поїздках це давало **25 розбіжностей**: 4 «бери» нижче цілі
> і 21 «не бери» вище неї (переважно довгі — ₴/км їх карала, ₴/год ні). Тепер
> обидві частини звіту дають однакову відповідь. `threshold_net_per_km` лишився
> **тільки** як основа «золотого правила» (`minGrossPerKm`), а не як критерій рішення.


### Модель часу — виміряна, не припущена
`cycle_model` калібрується (`npm run calibrate`) регресією **інтервалу між стартами
сусідніх замовлень** на дистанцію: наразі ≈ `10.19 + 1.96×км` (R≈0.47, n=149; місто
`10.07+1.94` n=130, тупик `12.9+1.82` n=19; темп ≈30.6 км/год). Цей інтервал —
реальна «вартість часу» замовлення: він **уже включає** подачу, чекання, передачу
і репозиціонування.

> ⚠️ **Порожняк назад до часу НЕ додається** — це подвійний рахунок (у місті
> наступне замовлення зазвичай приходить туди, де ти висадив). `empty_run_*`
> впливає **тільки на пальне**. Стара модель (`4 хв + пробіг×(1+порожняк)/швидкість`)
> давала утилізацію 118% — фізично неможливо — і вдвічі перебільшувала штраф за
> довгі поїздки.

**Пороги слотів не хардкодяться**: `lib.ts::deriveSlots` підбирає їх із даних
під свіжі `settings` (газ/комісія/ціль), а `slotsStats` бектестить на всіх `trips`.

> ⚠️ Шару «4 режими Автопілота» (`settings.modes`, `deriveModes`, `modePass`)
> **більше немає**. Він бектестив афінний поріг `A + B×км`, якого у формі Uklon
> не існує, тож звіт показував «екв. ₴/км», а міряв зовсім інше правило.
> Якщо в старих нотатках трапиться `deriveModes` — це мертве посилання.

> Важливо: **пороги ₴/км залежать від витрат (settings), а не від к-сті поїздок.**
> Додавання `trips` змінює лише **бектест** (скільки пройде, ₴/км після фільтра,
> «прибуткових відсічено», база). `generate` пише знімок у `.report-state.json`
> (gitignore) і показує в звіті панель **«Що змінилось з минулого запуску»**
> (`lib.ts::buildSnapshot` + `changesPanel` у `generate.ts`).

## Важелі фільтра «Автопілота» Uklon

> **Джерела правди** (у порядку надійності):
> 1. Офіційна стаття — https://uklon.com.ua/driver-update/knowledge-base/driver-update-filtry-ta-ih-nalashtuvannia/
>    Частина тексту схована за акордеонами («Натисніть тут, щоб переглянути
>    деталі», FAQ) — щоб дістати все, відкрий сторінку і зніми `innerText`
>    після розкриття прихованих блоків, інакше половини не буде.
> 2. Скріншоти форми фільтра від водія. **Папки `screens/` у репозиторії немає** —
>    вона локальна, поза git. Не шукай її; якщо потрібні деталі UI — питай.
> 3. Спостереження водія за поведінкою застосунку.

Один фільтр має: `Звідки` (радіус подачі, км — або сектор), `Куди` (сектори
призначення), `Тариф` — **Простий** (`Мін. вартість`, `Мін. ціна ₴/км`, тумблер
`Лише по місту`) і **Складний** (`Км у мінімалці`, `Мін. ціна ₴/км` окремо для
**міста** й **передмістя**), `Тип оплати`. **Максимальної дистанції поїздки
НЕМАЄ** — довгі ріжуться лише непрямо, через ₴/км.


> ⚠️ **Простий і Складний — взаємовиключні.** Заповнити можна обидві вкладки, але
> працює **тільки обрана**. Наші слоти й режими використовують `Км у мінімалці` та
> окрему ціну передмістя — це поля **Складного**. Якщо фільтр лишити в «Простому»,
> вони **мовчки ігноруються**, і вся конструкція розвалюється без жодної помилки.
> Крок «обери Складний» доданий в інструкцію звіту (`generate.ts::slotsPanel`).

> ⚠️ Наслідок: тумблер **`Лише по місту` живе у «Простому»** і **несумісний** із
> `Км у мінімалці` / ціною передмістя. Тобто `city_only` і `price_km_suburb` не
> можна поєднати в одному фільтрі — це або-або, а не два незалежні поля.

- Правило проходження (**інференс**, не цитата — див. попередження нижче):
  `сума ≥ Мін.вартість` **І** `сума ≥ ₴/км × max(дистанція, Км-у-мінімалці)`.
  Тому підняття `Км у мінімалці` б'є саме по **коротким** поїздкам.

> 🔴 **Офіційний опис суперечить сам собі — правило проходження не встановлене.**
> Стаття «Фільтри та їх налаштування» описує поля двома несумісними прикладами:
> - «**Км у мінімалці**»: «перші 2 км коштують 200 грн, **а далі — додаткова плата
>   за кожен км**» → **адитивна** модель `сума ≥ мін + ₴/км × max(0, d − K)`;
> - «**Мін. ціна км, місто**»: «якщо 12 грн/км, то за 5 км ви отримаєте мінімум
>   60 грн» → ціна за км множиться на **всю** дистанцію (наша модель).
>
> Адитивна модель **строго слабша**. На 164 поїздках (перевірено `/tmp/filter-rule.mjs`):
>
> | модель | приймає | ₴/год | нижче цілі серед прийнятих |
> |---|---|---|---|
> | наша (`max`) | 144 | **294** | 3 |
> | адитивна | 156 | 287 | **12** |
>
> Розбіжність — **12 поїздок, усі в глухі кути**, 220 ₴/год (нижче цілі 240).
> Вирівнювання `min_order = ₴/км × K` робить моделі тотожними **для міста**, але
> **не для передмістя** (там ціна за км інша, а мінімалка спільна) — тож захист від
> тупиків структурно ненадійний, якщо правило адитивне.
>
> **Дешевий експеримент:** слот «Середня подача» (мін. 80 ₴, 28 ₴/км, K=5) вимагає
> на 10 км **280 ₴** за нашою моделлю і **220 ₴** за адитивною. Якщо в зміні прийде
> замовлення ~240 ₴ на ~10 км — правило адитивне, і поріг передмістя треба піднімати
> (або вмикати whitelist секторів `Куди`).

- **Активних фільтрів максимум 3 в авторежимі + 3 на Ефір** (зі скрінів опису),
  працюють **незалежно один від одного**.
  Автофільтри об'єднуються за **АБО**: замовлення береться, якщо підходить під
  будь-який. Отже строгий фільтр нічого не «блокує» — він лише **додає**, і
  тримати його ввімкненим безкоштовно.

  > ⚠️ Публічна стаття формулює жорсткіше: «не більше **трьох** фільтрів»
  > без розділення на Авто і Ефір. При цьому там же описані **два лічильники**
  > активних фільтрів (окремо Автовзяття/Цикл і окремо Ефір), що радше підтримує
  > «3+3». Розбіжність **не вирішена** — рахувати ліміт як 3 на автослоти
  > (наш сетап у нього вкладається за будь-якого прочитання).
- Наслідок: потрібний поріг — гіпербола (`A/d + B`, короткі вимагають вищої ₴/км),
  а важіль плаский. **Але за АБО складати сходинку з різних `Км у мінімалці`
  не можна** — див. попередження в розділі про слоти. Сходинка виходить лише
  через **радіус подачі**.


### Режими роботи фільтра — поле самого Uklon (ground truth)
Не плутати з нашими «4 режимами Автопілота». Це критично для стратегії
«налаштував і забув»:

| режим | поведінка |
|---|---|
| **Авто** | **Одноразовий** пошук. Якщо ввімкнути під час виконання замовлення — пошук стартує при наближенні до точки висадки. **Після прийняття наступного замовлення або відмови фільтр вимикається.** |
| **Цикл** | Працює **безперервно**: кожне наступне замовлення після завершення попереднього. |
| **Ефір** | Фільтрує стрічку Ефіру. За замовчуванням в Ефірі діє **єдина умова — радіус подачі 3 км** відносно твого місцезнаходження. |

> ⚠️ **3 постійні слоти мають стояти в режимі `Цикл`, а не `Авто`.** В `Авто`
> фільтр гасне після кожного замовлення, і вся ідея «увімкнув і забув» ламається.
> Підтверджено офіційним FAQ: «якщо ви використовуєте режим «Авто», фільтр
> вимикається автоматично після кожного замовлення… Рекомендуємо «Цикл»».

> ⚠️ Будь-який автофільтр **вимикається**, якщо пропустити або відхилити
> **обов'язкове** замовлення. Тобто пропуск має ціну не лише в грошах: він гасить
> налаштування, і далі їдеш «без фільтра», поки не помітиш.

> ⚠️ **Фільтр — не гарантія.** Офіційний FAQ: «іноді ви можете побачити
> **додаткове замовлення** — це альтернатива простоюванню. Ви можете скасувати
> таке замовлення **без впливу на Активність**». Тобто Uklon сам підкидає
> замовлення **поза** фільтром, коли ти простоюєш. Наслідок для методики: бектест
> «слоти приймають N із 164» — це **нижня** оцінка потоку, а частина ризику
> «звузив фільтр → простій» вже погашена платформою.

> ⚠️ **Блок «Подача» (час подачі) з форми прибрали** — не плутати зі «Звідки
> (подача)», тобто **відстанню** до клієнта, яка лишилась. Прибраний блок стосувався
> запланованих замовлень («будь-який час»), радіус подачі до нього не має стосунку.

> **Дані самого Uklon про типову помилку** (з тієї ж статті): «до 90% водіїв, які
> користуються фільтрами, зберігають по 2 і більше фільтрів з **надто жорсткими
> умовами**. У результаті — немає відповідних замовлень, втрачено час і заробіток».
> Це рівно той ризик, який у нас квантифікує колонка «Треба заповнити»
> (`refillBreakeven`), і аргумент не задирати `target_net_per_hour`.


> Формулювання про Ефір («радіус подачі 3 км **відносно вашого місцезнаходження**»)
> прямо підтверджує семантику радіуса: коло рухається за водієм, це маршрутна
> відстань від поточної позиції до точки подачі.

**Золоте правило** (мін. валова ціна): `minGross = (threshold + breakeven) / (1 - commission)`, де
`breakeven = fuelPerKm*(1+empty_run_coef)`. Наразі ≈ **26.4 грн/км** у місті та
**31.4** у глухий кут (порожняк зональний, тож правило теж різне за зонами).

### 3 постійні слоти (практична настройка Автопілота)
Звіт містить панель **«3 постійні слоти»** — те, що реально вписати у
фільтри й більше не чіпати. Слоти **не хардкодяться**, а виводяться з даних:
`lib.ts::deriveSlots`. `slotPass` реалізує **інферовану** семантику фільтра
(`сума ≥ мін.вартість` І `сума ≥ ₴/км × max(дистанція, км-у-мінімалці)` І
`подача ≤ радіус`) — див. попередження про неоднозначність правила вище;
`slotsStats` рахує бектест об'єднання (АБО).

Поточна трійка (перегенерується разом зі звітом):

| слот | вкладка | мін. вартість | ₴/км місто | К | ₴/км передмістя | подача |
|---|---|---|---|---|---|---|
| Впритул | Простий | 80 | 22 | — | — (лише місто) | 1.5 км |
| Середня подача | Простий | 80 | 25 | — | — (лише місто) | 2 км |
| Далека подача | Складний | 80 | 28 | 5 | 36 | 2.5 км |

Бектест об'єднання: приймає **141 із 164**, **287 ₴/год** проти бази 267;
відсіяні — 187 ₴/год; «треба заповнити» **65%**.

> 🔴 **Вкладка тарифу — частина слота, а не константа.** `candidates` колись
> породжував кандидатів без ціни передмістя (→ `city_only`) з **будь-яким**
> `Км у мінімалці`, а картка звіту хардкодила «Тип тарифу: Складний». Разом це
> давало інструкцію, яку **неможливо ввести у форму**: тумблер «Лише по місту»
> є лише у **Простому**, а «Км у мінімалці» та ціна передмістя — лише у
> **Складному**. Тепер `city_only`-кандидати допускаються тільки з `K = 1`
> (це рівно те, що вводиться у Простий), а картка друкує ту вкладку, якій слот
> реально відповідає. Ціна виправлення — 1 замовлення й 1 ₴/год.
> Тест-запобіжник: «слот вводиться у форму Uklon».

**Слоти — драбина за подачею** (`ціна ↑ разом із радіусом ↑`):
- щабель 1 — **ціновий оптимум**: поріг ₴/км, що захоплює максимум надлишку
  `surplus = чистий − T×цикл/60`, з тим радіусом, який він заробив;
- щаблі 2–3 — найдешевший поріг, що заробляє радіус на 0.5 км ширший.

Тобто кожен наступний слот **строгіший за ціною, але дозволяє їхати далі** по
клієнта. Дешеве беремо лише впритул, за дороге — не шкода й проїхатись.

> ⚠️ **Чому НЕ «жадібна трійка», як було раніше.** Об'єднання за АБО бере
> **мінімум** порогів, тож кожен доданий фільтр може лише **послабити** відбір.
> За однакової ціни фільтр із більшим «км у мінімалці» — строга **підмножина**
> першого й не додає нічого. Стара версія саме так і вироджувалась: три слоти
> по 20 ₴/км, з яких другий додавав **1** замовлення, третій — **2**, а ставка
> не мінялась узагалі (294 ₴/год і з одним слотом, і з трьома). Формулювання
> «три фільтри складаються у сходинку, що наближає гіперболу» **невірне для
> АБО** — сходинка виходить лише тоді, коли слоти невкладені, а єдиний вимір,
> що це дає, — **радіус подачі**.

> ⚠️ **Поки `pickup_km` порожній, рядок «тільки цей слот» показує 0** для
> щаблів 2–3, бо `slotPass` пропускає перевірку подачі. Це **не** ознака, що
> вони зайві. Перевірено симуляцією (`pickup_km` підставлено синтетично):
> драбина починає додавати **+15 замовлень** із середньою подачею 2.4 км і
> сумою 183 ₴ — рівно цільовий сегмент «далеко, але добре платять».

Передмістя дозволяється лише за «безпечною» ціною `B + A/k` — тупики
єдині системно збиткові.

> ⚠️ **Чесний компроміс, який звіт показує явно.** Слоти піднімають ₴/год, але
> відсікають частину замовлень. Це вигідно **лише якщо звільнений час
> заповниться** новими замовленнями. З наявних даних це **не перевіряється**:
> ми бачимо тільки прийняті поїздки, а не ті, що фільтр відхилив би. Якщо на
> практиці почнуться простої — знижувати `target_net_per_hour`.

> ⚠️ **₴/год після фільтра — НЕ метрика для порівняння двох фільтрів.** Вона
> рахується лише серед **прийнятих**, тож строгіший набір виграє в ній
> **механічно**, навіть коли загальний заробіток менший. Порівнювати набори
> можна тільки через гроші за ту саму зміну:
> `netTotal(f) = passNet + f × freedH × phPass`, де `f` — частка заповнення.
> На цій помилці ми вже стояли — див. «Аудит методики».

### Метрика вибору цілі: `lib.ts::refillBreakeven`
Фільтр — обмін грошей на час: віддаєш `lostNet` ₴ відсіяних замовлень, отримуєш
`freedH` вільних годин. Обмін окупається, якщо
`freedH × phPass × fill ≥ lostNet` ⇒ **`fill = lostNet / (freedH × phPass)`** —
частка звільненого часу, яку треба заповнити, щоб вийти в нуль. Це єдине чесне
число для вибору `target_net_per_hour` (у звіті — колонка «Треба заповнити»).

Свіп по базі 164 поїздок (база **267 ₴/год**, перераховано `/tmp/fresh.mjs`
на `deriveSlots`+`refillBreakeven` зі свіжими `settings`):

| ціль | приймає | ₴/год | звільняє | треба заповнити |
|---|---|---|---|---|
| 220 | 89% (146) | 282 | 8.8 год | 63% |
| **240** | **86% (141)** | **287** | **12.3 год** | **65%** |
| 260 | 72% (118) | 301 | 22.8 год | 70% |
| 280 | 53% (87) | 312 | 31.4 год | 72% |
| 300 | 27% (45) | 342 | 45.6 год | 71% |

> ⚠️ **Аргумент за 240 слабший, ніж виглядав раніше.** Крива між 220 і 240
> плоска (**63% проти 65%**), тобто вибір між ними коштує ~5 ₴/год і 3.5 год
> зі зміни. Злам є, але вище: після **260** вимога стрибає до 71–72% і далі
> не покращується, тож за 280+ платиш **половиною зміни** (31.4 год із 59.3)
> за ставку, яка тримається лише в щільний попит. 240 лишається як точка
> з запасом на простої, але це **вибір ризику**, а не висновок із даних —
> переглянути, щойно з'явиться `data.idles`.

## Доменні висновки (перевірені на даних)
- Прибутковість **обернено залежить від дистанції**, але формулювання «12+ км
  збиткові» — **невірне**: у мінус не йде **жодна** поїздка з 164. Точно так:

  | група | n | ₴/год | вище цілі 240 | у мінус |
  |---|---|---|---|---|
  | <3 км | 37 | 300 | 97% | 0 |
  | 3–7 км | 81 | 284 | 81% | 0 |
  | 7–12 км | 34 | 252 | 56% | 0 |
  | 12+ км | 12 | 201 | 25% | 0 |

  «Збиткова» тут = **нижче цілі ₴/год**, тобто альтернативна вартість часу, а не
  збиток. Різниця важлива: у простій довгу поїздку брати варто.
- **Надбавка над тарифом — сильніший предиктор, ніж дистанція.** `надбавка =
  сума − (uklon_fare.base + per_km×км)`. Кореляція з ₴/год: **0.73** проти
  **−0.34** у дистанції. Працює **всередині кожної** групи дистанції, тобто це
  окремий важіль, а не переодягнена довжина:

  | група | з надбавкою ≥0 | з надбавкою <0 |
  |---|---|---|
  | <3 км | 357 ₴/год | 278 |
  | 3–7 км | 339 | 254 |
  | 7–12 км | 315 | 219 |
  | 12+ км | **301** | 209 |

  > Практичний наслідок: **12+ км із надбавкою (301 ₴/год) — вище цілі**, і брати
  > її нормально. Правило «довге = погане» треба читати як «довге **без надбавки**
  > = погане».

  ⚠️ Але як **важіль фільтра надбавка гірша** за поточні слоти: свіп по порогу
  надбавки дає рівні 73–76% «треба заповнити» на будь-якій відсічці (проти **65%**
  у слотів), бо разом із дешевими виносить забагато нормальних замовлень —
  на порозі 0 ₴ це 63/164 поїздок і більш ніж половина годин зміни. Тобто надбавка —
  **індикатор для ручного рішення в момент показу**, а не поле фільтра.
  (Свіп рахувався на попередній калібровці циклу; після рекалібрування рівні
  зсунулись, але порядок «гірше за слоти» лишився — переміряти, якщо знадобиться точність.)
- Тип оплати майже не впливає (готівка ≈ безготівка по чист/км); вирішують **дистанція + глухий кут**.
- Глухі кути біля Вінниці: Стрижавка, Сабарів, Агрономічне, Десна, Гавришівка, Вінницькі Хутори,
  Якушинці, Зарванці (список у `settings.dead_end_areas`).
- **Сектори призначення у фільтрі не обмежуємо** — відсів робить ціна. Але
  напрямок помилки **змінився на протилежний**: з 20 тупиків слоти
  пропускають тепер **1**, і з **4** прибуткових (≥240 ₴/год: 254, 260, 261, 320)
  спіймано лише **1**. Збиткових не просочилось **жодного**, відсічені
  тупики — **179 ₴/год**.

  > ⚠️ Тобто ми більше не «ловимо рівно прибуткові», а **ріжемо із запасом**:
  > помилка пішла в бік **упущеної вигоди**, а не ризику. Лікується це
  > **зниженням** ₴/км для передмістя, а **не** whitelist'ом секторів — сектори
  > вимкнуть ці замовлення остаточно. Блок `report/panels/filters.ts::sectorsNote`
  > тепер розрізняє ці два режими відмови в тексті (раніше він в обох випадках
  > радив «підняти ₴/км», що для нашого випадку — рівно навпаки).

  Дальняк не проходить і за ціною (0 з 1). Whitelist секторів лишається запасним
  варіантом на випадок, коли напрямок поганий не через гроші, а через дорогу.

  > ⚠️ Цей висновок тримається на **нашій** інтерпретації правила фільтра (`max`).
  > Якщо правило виявиться **адитивним** (див. попередження вище), крізь ціновий
  > поріг просочуються **12 тупиків по 220 ₴/год**, і whitelist секторів `Куди`
  > перестає бути запасним варіантом. Тобто експеримент із 240 ₴ на 10 км —
  > це не педантизм, а перевірка, чи потрібні нам сектори взагалі.

- **Порожняк назад із тупика — міф (перевірено, не повторювати).** Гіпотеза «після
  глухого кута довго стоїш без замовлення» на 164 поїздках **не підтвердилась**:
  - залишок «факт − модель циклу» — місто **−0.4 хв**, тупик **+2.6 хв**;
    різниця **2.9 хв** (похибка ±1.5, t=1.93, бутстреп 95% `[0; 5.8]`);
  - відсічка `GAP_MAX=60` нічого не ховає: **0 із 19** тупиків мали інтервал >60 хв
    (усі три довгі паузи в масиві — після *міських* поїздок);
  - зміна тупиком не закінчується: **14 змін — 14 останніх поїздок міські**,
    тупиків серед них 0 при базовій частці 11.6%.

  Тупики справді гірші (187 проти 283 ₴/год), але причина — **дистанція**
  (середній тупик 8 км), і її ріже звичайний поріг ₴/км. Поправка на хвіст
  опускає його ще на ~20 ₴/год — обидва числа й так нижче цілі 240, **жодне
  рішення не змінюється**. Тому: секторів у фільтр не додаємо, зворотний
  пробіг НЕ збираємо — 3 хвилини не окупляють зміни методики збору.

## Аудит методики (що перевірено і де діри)
- **Слоти не переоптимізовані — але вибір алгоритму має ціну.** `deriveSlots`
  підбирає слоти на тих самих рядках, якими потім міряє `slotsStats` —
  класичний in-sample. Переміряно на 200 випадкових розбиттях 60/40 **по
  змінах** (плюс LOSO по 15 змінах):

  | варіант відбору | in-sample | out-of-sample | приймає | LOSO |
  |---|---|---|---|---|
  | старий (argmax) | 291 | **292.6** `[283; 302]` | 82% | 293.1 |
  | новий (м'яке плато) | 287 | 287.4 `[278; 297]` | 87% | 286.1 |

  Оптимізм малий в обох (OOS не нижче in-sample) — **метод здоровий**.
  Але «старий виграє 5 ₴/год» — саме та пастка, про яку попереджено вище:
  він просто строгіший (136 проти 141 прийнятих). За грошима за ту саму
  зміну: при заповненні `f = 0` новий дає **+678 ₴**, при `f = 1` — **−238 ₴**;
  вибір перевертається на **f = 74%**. Оскільки `f` не виміряний (`data.idles`
  порожній), тримаємо **м'яке плато** — воно менше втрачає в поганому
  сценарії. Переглянути, щойно з'являться журнали простоїв.
  > Різниця — 7 поїздок: новий додає 6 довгих міських (217–243 ₴/год, усі
  > нижче цілі), старий — 1 прибутковий тупик (254 ₴/год).
- ⚠️ **Бутстреп у відборі слотів шкодив — прибраний.** Абляція (60 розбиттів):

  | варіант | OOS ₴/год | приймає | стабільність ціни 1-го слота |
  |---|---|---|---|
  | старий (argmax) | 292.6 | 82% | 24 у 42/60 |
  | бутстреп + argmax | 291.9 | 83% | 24 у **24/60** (8 різних цін 18–28) |
  | бутстреп + м'яке плато | 287.6 | 87% | 22 у 32/60 |
  | **без бутстрепу + м'яке плато** | **287.4** | **87%** | **22 у 38/60** |

  Бутстреп не додавав нічого до якості (різниця 0.2 ₴/год — шум), але
  **погіршував стабільність** і коштував 60× перерахунку. Залишений ЛИШЕ як
  діагностика (`Slot.stability.agreePct` — показує впевненість, не впливає на
  вибір). Не повертати в `pickPlateau` без нового виміру.
- ⚠️ **Утилізація більше НЕ є перевіркою моделі.** `shiftStats` дає **100%**
  (59.3 год модельних циклів на 59.3 год змін), і це **тавтологія**, а не збіг:
  `realH` рахується від першого до останнього старту зміни, а `cycle_model`
  калібрується регресією рівно на цих самих інтервалах — сума модельних циклів
  зобов'язана дорівнювати сумі інтервалів. Стара цифра **89.7%** (і 118% у
  доісторичної `time_model`) походила з іншого способу рахувати `realH`.
  Тобто «модель сходиться з реальністю» тепер треба доводити чимось іншим —
  наприклад, фактичною тривалістю змін із журналу, якого поки немає.
- **Реальність зміни** (15 змін): медіана ≈ **10 поїздок, 3.4 год, 986 ₴**
  (модельних 59.3 год на 164 поїздки, база 267 ₴/год).
  Довгі зміни дають гіршу ставку (≥8 год → 231 ₴/год, <8 год → 268; кореляція
  тривалості з ₴/год **−0.48**), але це **3 зміни проти 9** — сигнал слабкий.
  Всередині зміни втоми не видно: перша третина 268 → остання 294 ₴/год, тобто
  падіння дає не втома, а те, що довгі зміни випадають на слабкий попит.
- **Година виїзду — слабкий важіль.** Профіль рівний (222–312 ₴/год), вираженого
  вікна немає; найгірше 11:00 (222), найкраще 21:00 (312), але n малі. Надбавка
  за годинами теж не має стабільного піку. Планувати графік за цим не варто.
- **Концентрація помірна**: топ 20% поїздок дають 55% надлишку над ціллю, топ
  50% — 89%. Тобто середнє не тримається на кількох викидах.
- ⚠️ **Діра: `max_pickup_km` не бектеститься.** `pickup_km` заданий у **0 із 164**
  поїздок, тому `slotPass` пропускає перевірку подачі повністю.
  Радіус подачі у слотах (зараз **1.5 / 2 / 2.5 км**) виводиться з ціни
  (`earnedPickupKm`), а не з даних: ми не знаємо ні скільки замовлень втратимо
  звуженням, ні скільки часу зекономимо. Пальне подачі при цьому враховане
  (порожняк 0.3 × середні 5.9 км ≈ 1.8 км уже закладено), і час теж —
  `cycle_model` виміряна з інтервалів між стартами, тож подача в ній є.
  Додавати подачу до часу/газу окремо — **подвійний рахунок**. Щоб важіль став
  керованим, треба збирати `pickup_km`.


## Подача (`pickup_km`) — семантика, ціна, збір

**Що таке «Звідки → Відстань» у фільтрі** (підтверджено двічі): Uklon рахує
**відстань маршруту** від поточної позиції водія до точки, де забирати клієнта —
офіційне формулювання «подача по відстані: **розраховується з врахуванням
розв'язок**», тобто це дорожня, а не пряма відстань. Коло рухається за тобою.
Отже вся економіка нижче застосовна прямо. Альтернатива в тому ж полі —
**подача по сектору** (тільки з вибраного сектору), її не використовуємо.


**Ціна подачі** — `lib.ts::pickupCostPerKm`:
```
pickupCostPerKm = fuelPerKm + target_net_per_hour × cycle_model.per_km_min / 60
```
Наразі ≈ **13.73 ₴/км** (пальне 5.89 + час 7.84).

**Нуль відліку** — `lib.ts::typicalPickupKm`: типова подача **вже врахована**
(порожняк 0.3 × середні 5.9 км ≈ **1.76 км** у пальному; час — у виміряному циклі).
Тому питання завжди «скільки км ПОНАД типову», а не «з нуля». Коли назбирається
≥20 фактичних `pickup_km`, функція автоматично перейде на факт.

**Радіус заробляється ціною** — `lib.ts::earnedPickupKm`: беремо 10-й перцентиль
надлишку над ціллю серед замовлень, які слот пропускає **за ціною**
(`slotPassPrice`, без перевірки подачі — інакше колове визначення), ділимо на
ціну км. Радіус витримують ~90% потоку слота.

| ₴/км | заслужений радіус (90% потоку) |
|---|---|
| 22 | **1.5 км** |
| 25 | 2.0 км |
| 28 (К=5) | 2.5 км |

> Практичний висновок: **радіус не можна призначати окремо від ціни.** Раніше
> слоти мали захардкоджені 1.5/3/3 км — це була вигадка. На 20 ₴/км заробляється
> лише 2 км; 3 км треба «купити» вищим порогом.

**Пастка АБО** (виправлено в `fc6208c`, лишається як застереження). Радіус слота
обмежує лише ті замовлення, які приймає **тільки він один**. Стара версія
`deriveSlots` видавала три майже однакові слоти (всі 20 ₴/км, різниця лише в
«км у мінімалці») — 143 із 164 замовлень підходили під 2+ слоти, ексклюзивних
було 4, тож жорсткий радіус на одному слоті не різав нічого: ширший пропускав.
Тому ціна й радіус мусять рости **разом**: дешевий слот → тільки впритул,
далека подача → тільки за високий поріг.


**Ручна політика радіуса (до налаштування фільтрів)**: у глухому куті ставив
**ширше** (до 4 км), у живій зоні **2 км** (3 у затишшя). Логіка зворотна до
«ціни км»: у тупику мало замовлень, тож вузький радіус = простій. Тобто радіус
балансує не лише вартість, а й **доступність** замовлень — цього боку наша
модель поки не бачить.

> ⚠️ **Поправка до висновку про тупики.** Результат «після тупика хвіст лише
> +2.6 хв» отримано на даних, де водій **компенсував** тупики ширшим радіусом
> (до 4 км). Ця зайва подача сидить усередині виміряного інтервалу, тож висновок
> «тупики не дорожчі за часом» справедливий **за умови такої компенсації**, а не
> безумовно. Частину ціни тупика сплачено кілометрами подачі, яких ми не бачимо.
> Розділити одне від одного можна буде лише зібравши `pickup_km`.

**Збір**: поля `pickup_km` і `pickup_min` є в `types.ts::Trip` — записуються
вручну з екрана пропозиції (Uklon показує і відстань, і час підльоту, напр. «7 хв»).
`slotPass` почне перевіряти подачу автоматично, щойно `pickup_km`
з'явиться (поки `null` — перевірка пропускається). Достатньо **20–30 замірів**,
щоб `typicalPickupKm` перейшла на факт і радіус перестав бути здогадкою.
Пара «км + хв» додатково розділяє «далеко» і «пробка» та калібрує темп руху
напряму, без регресії.

```json
{ "datetime": "22.08 18:40", "payment": "Готівка", "amount": 150,
  "distance": 4.2, "pickup_km": 1.8, "pickup_min": 7,
  "from": "...", "to": "...", "zone": "Місто" }
```

### Простої (`data.idles`) — ціна відмови
Останнє, чого модель не бачить: скільки коштує **звуження** фільтра. Відхилені
фільтром замовлення нам не видно в принципі, тому єдине джерело — ручний запис
простоїв (`types.ts::Idle`, масив `data.idles`):

```json
{ "datetime": "22.08 19:15", "minutes": 25, "zone": "Глухий кут", "radius_km": 2 }
```

Навіщо кожне поле:
- `minutes` + `datetime` — коли і скільки стояв (година доби важлива: попит нерівний).
- `zone` — **закриває питання про тупики**, яке на самих поїздках не вирішується:
  зараз ми бачимо лише інтервали між прийнятими замовленнями, а не те, скільки
  часу простояв, не отримавши нічого.
- `radius_km` — який радіус стояв у фільтрі під час простою. Це **єдиний спосіб**
  виміряти, скільки простою купує вузький радіус, і перевести вибір «2 чи 3 км»
  з інтуїції в розрахунок.

## Конвенції
- Мова спілкування й коментарів — **українська**.
- Числа: відстань `км` з 2 знаками, суми `грн`.
- Дата/час у `datetime`: `DD.MM HH:MM` (місяць — серпень `.08`).
- Не коммітити `report.html`, `*.xlsx`, `node_modules/`, `.DS_Store`, `.idea/` (див. `.gitignore`).
- Скрипти мають лишатися ідемпотентними: `generate`/`update-pay` можна запускати повторно.
- Формули живуть **в одному місці** — `src/lib.ts`. `generate/analyze/report` імпортують `compute` звідти.
- **Не запускати великі скрипти через `node -e` в терміналі.** Багаторядкову логіку
  писати у виконуваний файл (`.ts`/`.mjs`) і запускати його. Одноразові аналізи —
  у `/tmp/*.mjs` (поза репо); постійні інструменти — у `src/`. Термінал лишати для
  коротких однорядкових команд.

## Пастки середовища (наступали, не наступати знову)

- 🔴 **Емодзі в `src/*.ts` періодично зникають.** Двічі за сесію робоче дерево
  виявлялось «зміненим» без жодної правки: з `generate.ts` (і раз із `lib.ts`)
  вирізало емодзі, лишивши модифікатор варіації — `🏘️ Місто` → `️ Місто`,
  `💡 Авто-інсайти` → ` Авто-інсайти`. Логіка не чіпається, тільки символи.
  **Перед роботою завжди перевіряй `git status`**; якщо диф — суцільні видалення
  емодзі, це воно: `git checkout -- <файли>`, не комітити. Джерело не знайдене
  (ймовірно інструмент/редактор ріже символи поза BMP при збереженні).
- 🔴 **Термінал ламає багаторядкові команди.** `git commit -m` з апострофом
  (`об'єднання`) і heredoc (`<<'EOF'`) обидва спотворюються — текст склеюється,
  а heredoc може лишити сесію висіти в режимі вводу. **Повідомлення коміту писати
  у файл** (`/tmp/msg.txt`) і комітити через `git commit -F /tmp/msg.txt`.
  Файл створювати інструментом запису файлів, а не `cat > ... <<EOF`.
- CSS у `generate.ts`: модифікатори (`-bottom`, `-top`, `-sm`) подекуди оголошені
  **вище** своїх базових класів. За однакової специфічності виграє те, що нижче,
  тож правка модифікатора може тихо не спрацювати. Уже обпеклись на
  `.fx-warn-bottom` (реальний відступ був 0). Лікується підвищенням специфічності
  (`.fx-warn.fx-warn-bottom`). Перевіряти **обчисленими стилями** в браузері, не на око.


## Обмеження
- OCR/детекція оплати — лише **macOS** (Apple Vision + jimp).
- `src/updatePay.ts` зіставляє скрін↔поїздку за **відбитком «сума + дистанція»**
  (не за датою!): свіжі замовлення застосунок показує **без дати**, лише з часом.
  Пара сума+км практично унікальна (163/164 на базі); рідкісні колізії розрізняються
  часом `HH:MM` зі скріна (перші 3 рядки пропускаються — там годинник статус-бара).
  Поїздки все одно треба додати в `data.json` заздалегідь (`npm run parse`).

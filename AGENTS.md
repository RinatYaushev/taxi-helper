# AGENTS.md

Контекст і правила для AI-агентів (Copilot тощо), що працюють у цьому репозиторії.

## Про проєкт

`taxi-helper` — персональний калькулятор прибутковості поїздок у таксі **Uklon**.
Мета — максимізувати чистий прибуток через відсів невигідних замовлень і власні
фільтри в «Автопілоті».

### Профіль водія (важливо для розрахунків)
- Місто: **Вінниця** (компактне, короткі плечі, є «глухі кути» — навколишні села).
- Авто: **Ford Fusion 2.5** (атмосферник) з **ГБО** → дешевий кілометр на газі.
- Графік: вечори буднів + вихідні (піковий/вечірній попит).
- Витрата газу: ~13–14 л/100 км. Ціна газу змінна (задається в `settings`).

## Архітектура даних

Єдине джерело правди — **`data.json`** (`settings` + масив `trips`).
Усе інше — генератори/аналізатори поверх нього. HTML-звіт — похідний артефакт (в git не коммітиться).

Схема поїздки:
```json
{ "datetime": "16.08 19:53", "payment": "Безготівка", "amount": 185,
  "distance": 6.23, "pickup_km": 1.2, "from": "...", "to": "...", "zone": "Місто" }
```
- `payment`: `Готівка` | `Безготівка` | `Комбінована`
- `zone`: `Місто` | `Глухий кут` — визначається **за адресою призначення** (`to`).
- `pickup_km` *(опційно)*: відстань подачі до клієнта, км. Старі поїздки без цих
  даних; у бектесті режимів подача перевіряється лише коли `pickup_km` заданий.

## Ключові команди

```bash
npm install             # залежності (jimp)
npm run generate        # data.json -> report.html (самодостатній HTML-звіт)
npm run analyze         # per-trip грн/км, чист/км
npm run report          # зведення по дистанції/зоні/годині/оплаті
npm run parse           # /tmp/ocr.txt -> нові поїздки в data.json (--write щоб записати)
npm run update-pay      # оновлення payment за кольором іконки
./pipeline.sh [dir]     # повний конвеєр обробки скрінів (default ~/Desktop/screens)
```

> Стек: **TypeScript на Node.js 22+** (запуск напряму, без збірки — Node сам стрипає
> типи). Пиши **erasable** TS: без `enum`/`namespace`/parameter-properties.
> Локальні імпорти — з розширенням `.ts` (`import ... from "./lib.ts"`).

## Обробка скріншотів замовлень (macOS)

1. `swift ocr.swift <dir>` → текст (адреси, `сума ₴`, `відстань км`, `DD серп. HH:MM`).
2. `swift ocr_boxes.swift <dir>` → ті ж рядки з нормалізованими bounding-box (origin — низ-ліво).
3. `npm run update-pay` (`src/updatePay.ts`) → визначає **тип оплати за кольором іконки**
   ліворуч від суми:
   - 🟡 жовтий (H≈38–62°) → **Готівка**
   - 🟢 зелений (H≈90–165°) → **Комбінована** (гаманець: частина готівка + частина на баланс)
   - 🔵 синій (H≈190–255°) → **Безготівка**
   Іконка — суцільне кольорове коло, ~1500–2000 насичених пікселів; сигнал чистий і однозначний.

### Як додавати НОВІ поїздки зі скрінів
Автоматично через `src/parseOrders.ts` (`npm run parse`): читає `/tmp/ocr.txt`,
витягує `datetime/amount/distance/from/to` за якорями (дата, `сума ₴`, `відстань км`),
зливає перенесені адреси, визначає `zone` через `inArea(to, dead_end_areas)`, дедупить
за `datetime`. Без прапорця — dry-run; `--write` дописує в `trips`. Тип оплати потім
проставляє `src/updatePay.ts` за кольором іконки.
> Стійкість OCR: сума нормалізується проти шуму іконки оплати зліва (напр. `9111`→`111`,
> бо міські суми — 2–3 цифри, <1200 грн); `км` матчиться без регістру; сума ловиться навіть
> коли OCR розбив `210 ₴` на `210` + `) ₴`. Проблемні блоки парсер друкує окремо — перевіряй вручну.
> **Звірка суми двома джерелами:** верхнє число біля `₴` та рядок розбивки «Оплата замовлення»
> (`66 ₴ готівкою, 45 ₴ на баланс`) узгоджуються між собою — кожне має свій режим відмови
> (верх: шум іконки; розбивка: OCR обрізає частину, напр. `260 готівкою` без `35 на баланс`).
> Якщо збігаються — тип оплати береться з розбивки (готівка/баланс/обидва → Готівка/Безготівка/Комбінована);
> якщо ні, а верх «чистий» (40–1200) — віримо верху; якщо верх зашумлений (>1200) — розбивці.
> `update-pay` за кольором іконки лишається фінальним джерелом типу оплати.

## Формули (єдине джерело — `src/lib.ts::compute`)
```
fuelPerKm  = gas_consumption_l_100km/100 * gas_price_per_l
gas        = distance * (1 + empty_run_coef) * fuelPerKm
commission = amount * commission_uklon_pct/100  (+ commission_cashless_pct% якщо Безготівка)
net        = amount - commission - gas
netPerKm   = net / distance
timeMin    = cycle_model.base_min + per_km_min * distance      ← ВИМІРЯНА
netPerHour = net / (timeMin/60)
```
Рекомендація: `netPerKm ≥ threshold_net_per_km` → **бери**; `≥ marginal_net_per_km` → **думай**; інакше **пропускай**.

### Модель часу — виміряна, не припущена
`cycle_model` калібрується (`npm run calibrate`) регресією **інтервалу між стартами
сусідніх замовлень** на дистанцію: наразі ≈ `10.6 + 1.79×км` (R²≈0.54; місто
`10.6+1.73`, тупик `12.9+1.82`). Цей інтервал — реальна «вартість часу» замовлення:
він **уже включає** подачу, чекання, передачу і репозиціонування.

> ⚠️ **Порожняк назад до часу НЕ додається** — це подвійний рахунок (у місті
> наступне замовлення зазвичай приходить туди, де ти висадив). `empty_run_*`
> впливає **тільки на пальне**. Стара модель (`4 хв + пробіг×(1+порожняк)/швидкість`)
> давала утилізацію 118% — фізично неможливо — і вдвічі перебільшувала штраф за
> довгі поїздки (12+ км: 146 ₴/год замість реальних ~223).

**Пороги режимів «Автопілота»** (`settings.modes`) не хардкодяться: кожен режим
задає `price_km_mult` (і опційно `price_km_suburb_mult`), а `lib.ts::deriveModes`
рахує `min_price_km_city = round(minGrossPerKm × price_km_mult)`. Тож пороги ₴/км
**перераховуються** зі свіжих `settings` (газ/комісія), а бектест — на всіх `trips`.

> Важливо: **пороги ₴/км залежать від витрат (settings), а не від к-сті поїздок.**
> Додавання `trips` змінює лише **бектест** (скільки пройде, ₴/км після фільтра,
> «прибуткових відсічено», база). `generate` пише знімок у `.report-state.json`
> (gitignore) і показує в звіті панель **«Що змінилось з минулого запуску»**
> (`lib.ts::buildSnapshot` + `changesPanel` у `generate.ts`).

## Важелі фільтра «Автопілота» Uklon (ground truth зі скрінів)
Один фільтр має: `Звідки` (радіус подачі, км + сектори), `Куди` (сектори призначення),
`Тариф` — **Простий** (`Мін. вартість`) і **Складний** (`Км у мінімалці`,
`Мін. ціна ₴/км` окремо для **міста** й **передмістя**), тумблер `Лише по місту`,
`Тип оплати`. **Максимальної дистанції поїздки НЕМАЄ** — довгі ріжуться лише
непрямо, через ₴/км.

- Правило проходження (інференс із поведінки, не з документації):
  `сума ≥ Мін.вартість` **І** `сума ≥ ₴/км × max(дистанція, Км-у-мінімалці)`.
  Тому підняття `Км у мінімалці` б'є саме по **коротких** поїздках.
- **Активних фільтрів максимум 3 в авторежимі + 3 на Ефір** (офіційний опис,
  скріни в `screens/filters-desc`), працюють **незалежно один від одного**.
  Автофільтри об'єднуються за **АБО**: замовлення береться, якщо підходить під
  будь-який. Отже строгий фільтр нічого не «блокує» — він лише **додає**, і
  тримати його ввімкненим безкоштовно.
- Наслідок: потрібний поріг — гіпербола (`A/d + B`, короткі вимагають вищої ₴/км),
  а важіль плаский. Три фільтри з різними `Км у мінімалці` складаються у **сходинку**,
  що наближає її.

### Режими роботи фільтра — поле самого Uklon (ground truth)
Не плутати з нашими «4 режимами Автопілота». Це критично для стратегії
«налаштував і забув»:

| режим | поведінка |
|---|---|
| **Авто** | **Одноразовий** пошук. Якщо ввімкнути під час виконання замовлення — пошук стартує при наближенні до точки висадки. **Після прийняття наступного замовлення або відмови фільтр вимикається.** |
| **Цикл** | Працює **безперервно**: кожне наступне замовлення після завершення попереднього. |
| **Ефір** | Фільтрує стрічку Ефіру. За замовчуванням в Ефірі діє **єдина умова — радіус подачі 3 км** відносно твого місцезнаходження. |

> ⚠️ **3 постійні слоти мають стояти в режимі `Цикл`, а не `Авто`.** В `Авто`
> фільтр гасне після кожного замовлення, і вся ідея «увімкнув і забув» ламається.

> ⚠️ Будь-який автофільтр **вимикається**, якщо пропустити або відхилити
> **обов'язкове** замовлення. Тобто пропуск має ціну не лише в грошах: він гасить
> налаштування, і далі їдеш «без фільтра», поки не помітиш.

> Формулювання про Ефір («радіус подачі 3 км **відносно вашого місцезнаходження**»)
> прямо підтверджує семантику радіуса: коло рухається за водієм, це маршрутна
> відстань від поточної позиції до точки подачі.

**Золоте правило** (мін. валова ціна): `minGross = (threshold + breakeven) / (1 - commission)`, де
`breakeven = fuelPerKm*(1+empty_run_coef)`. Наразі ≈ **28 грн/км**.

### 3 постійні слоти (практична настройка Автопілота)
Крім 4 «навчальних» режимів (`settings.modes`) звіт містить окрему панель
**«3 постійні слоти»** — те, що реально вписати у фільтри й більше не чіпати.
Слоти **не хардкодяться**, а виводяться з даних: `lib.ts::deriveSlots`.
`slotPass` реалізує точну семантику фільтра (`сума ≥ мін.вартість` І
`сума ≥ ₴/км × max(дистанція, км-у-мінімалці)` І `подача ≤ радіус`),
`slotsStats` рахує бектест об'єднання (АБО).

**Слоти — драбина за подачею** (`ціна ↑ разом із радіусом ↑`):
- щабель 1 — **ціновий оптимум**: поріг ₴/км, що захоплює максимум надлишку
  `surplus = чистий − T×цикл/60`, з тим радіусом, який він заробив;
- щаблі 2–3 — найдешевший поріг, що заробляє радіус на 0.5 км ширший.

Тобто кожен наступний слот **строгіший за ціною, але дозволяє їхати далі** по
клієнта. Дешеве беремо лише впритул, за дороге — не шкода й проїхатись.

> ⚠️ **Чому НЕ «жадібна трійка», як було раніше.** Об'єднання за АБО бере
> **мінімум** порогів, тож кожен доданий фільтр може лише **послабити** відбір.
> За однакової ціни фільтр із більшим «км у мінімалці» — строга **підмножина**
> першого й не додає нічого. Стара версія саме так і вироджувалась: три слоти
> по 20 ₴/км, з яких другий додавав **1** замовлення, третій — **2**, а ставка
> не мінялась узагалі (294 ₴/год і з одним слотом, і з трьома). Формулювання
> «три фільтри складаються у сходинку, що наближає гіперболу» **невірне для
> АБО** — сходинка виходить лише тоді, коли слоти невкладені, а єдиний вимір,
> що це дає, — **радіус подачі**.

> ⚠️ **Поки `pickup_km` порожній, рядок «тільки цей слот» показує 0** для
> щаблів 2–3, бо `slotPass` пропускає перевірку подачі. Це **не** ознака, що
> вони зайві. Перевірено симуляцією (`pickup_km` підставлено синтетично):
> драбина починає додавати **+15 замовлень** із середньою подачею 2.4 км і
> сумою 183 ₴ — рівно цільовий сегмент «далеко, але добре платять».

Передмістя дозволяється лише за «безпечною» ціною `B + A/k` — тупики
єдині системно збиткові.

> ⚠️ **Чесний компроміс, який звіт показує явно.** Слоти піднімають ₴/год, але
> відсікають частину замовлень. Це вигідно **лише якщо звільнений час
> заповниться** новими замовленнями. З наявних даних це **не перевіряється**:
> ми бачимо тільки прийняті поїздки, а не ті, що фільтр відхилив би. Якщо на
> практиці почнуться простої — знижувати `target_net_per_hour`.

### Метрика вибору цілі: `lib.ts::refillBreakeven`
Фільтр — обмін грошей на час: віддаєш `lostNet` ₴ відсіяних замовлень, отримуєш
`freedH` вільних годин. Обмін окупається, якщо
`freedH × phPass × fill ≥ lostNet` ⇒ **`fill = lostNet / (freedH × phPass)`** —
частка звільненого часу, яку треба заповнити, щоб вийти в нуль. Це єдине чесне
число для вибору `target_net_per_hour` (у звіті — колонка «Треба заповнити»).

Свіп по базі 164 поїздок (база 276 ₴/год):

| ціль | приймає | ₴/год | звільняє | треба заповнити |
|---|---|---|---|---|
| 220 | 92% | 291 | 6.3 год | 54% |
| **240** | **90%** | **294** | **8.8 год** | **60%** |
| 260 | 75% | 307 | 19.4 год | 70% |
| 280 | 60% | 319 | 27.1 год | 71% |
| 300 | 40% | 338 | 37.3 год | 72% |

> Обрано **240**: віддає лише 10% замовлень і 8.8 год зі зміни, дає +18 ₴/год.
> Вище 260 крива ламається — вимога до заповнення стрибає до ~70% і далі не
> покращується, тобто за 280+ платиш половиною зміни за ставку, яка тримається
> тільки в щільний попит. 240 — точка, де ще є запас на простої.

## Доменні висновки (перевірені на даних)
- Прибутковість **обернено залежить від дистанції**, але формулювання «12+ км
  збиткові» — **невірне**: у мінус не йде **жодна** поїздка з 164. Точно так:

  | група | n | ₴/год | вище цілі 240 | у мінус |
  |---|---|---|---|---|
  | <3 км | 37 | 299 | 97% | 0 |
  | 3–7 км | 81 | 291 | 93% | 0 |
  | 7–12 км | 34 | 263 | 79% | 0 |
  | 12+ км | 12 | 223 | 50% | 0 |

  «Збиткова» тут = **нижче цілі ₴/год**, тобто альтернативна вартість часу, а не
  збиток. Різниця важлива: у простій довгу поїздку брати варто.
- **Надбавка над тарифом — сильніший предиктор, ніж дистанція.** `надбавка =
  сума − (uklon_fare.base + per_km×км)`. Кореляція з ₴/год: **0.73** проти
  **−0.34** у дистанції. Працює **всередині кожної** групи дистанції, тобто це
  окремий важіль, а не переодягнена довжина:

  | група | з надбавкою ≥0 | з надбавкою <0 |
  |---|---|---|
  | <3 км | 357 ₴/год | 278 |
  | 3–7 км | 339 | 254 |
  | 7–12 км | 315 | 219 |
  | 12+ км | **301** | 209 |

  > Практичний наслідок: **12+ км із надбавкою (301 ₴/год) — вище цілі**, і брати
  > її нормально. Правило «довге = погане» треба читати як «довге **без надбавки**
  > = погане».

  ⚠️ Але як **важіль фільтра надбавка гірша** за поточні слоти: свіп по порогу
  надбавки дає рівні 73–76% «треба заповнити» на будь-якій відсічці (проти **60%**
  у слотів), бо разом із дешевими виносить забагато нормальних замовлень —
  на порозі 0 ₴ це 63/164 поїздок і 35.8 з 57.7 год зміни. Тобто надбавка —
  **індикатор для ручного рішення в момент показу**, а не поле фільтра.
- Тип оплати майже не впливає (готівка ≈ безготівка по чист/км); вирішують **дистанція + глухий кут**.
- Глухі кути біля Вінниці: Стрижавка, Сабарів, Агрономічне, Десна, Гавришівка, Вінницькі Хутори,
  Якушинці, Зарванці (список у `settings.dead_end_areas`).
- **Сектори призначення у фільтрі не обмежуємо** — відсів робить ціна. Перевірено на
  164 поїздках (ціль 240 ₴/год): з 18 тупиків слоти пропускають 4, і це рівно ті
  4, що дають ≥ цілі (254–320 ₴/год); жоден збитковий не просочився, відсічені
  тупики — 170 ₴/год. Дальняк не проходить і за ціною (300 ₴ / 14.2 км ≈ 21 ₴/км
  проти потрібних 33–45). Whitelist секторів — бінарний важіль: вимикає район
  разом із рідкісними дорогими замовленнями туди, тому лишається запасним
  варіантом (пролізли збиткові тупики / поганий напрямок не через гроші, а через
  дорогу). Блок `generate.ts::sectorsNote` рахує цю якість класифікації на
  свіжих даних після кожного `npm run generate`.
- **Порожняк назад із тупика — міф (перевірено, не повторювати).** Гіпотеза «після
  глухого кута довго стоїш без замовлення» на 164 поїздках **не підтвердилась**:
  - залишок «факт − модель циклу» — місто **−0.4 хв**, тупик **+2.6 хв**;
    різниця **2.9 хв** (похибка ±1.5, t=1.93, бутстреп 95% `[0; 5.8]`);
  - відсічка `GAP_MAX=60` нічого не ховає: **0 із 19** тупиків мали інтервал >60 хв
    (усі три довгі паузи в масиві — після *міських* поїздок);
  - зміна тупиком не закінчується: **14 змін — 14 останніх поїздок міські**,
    тупиків серед них 0 при базовій частці 11.6%.

  Тупики справді гірші (206 проти 284 ₴/год), але причина — **дистанція**
  (середній тупик 8 км), і її ріже звичайний поріг ₴/км. Поправка на хвіст
  опускає 206 до ~185 — обидва числа й так нижче цілі 240, **жодне рішення не
  змінюється**. Тому: секторів у фільтр не додаємо, зворотний пробіг НЕ збираємо —
  3 хвилини не окупають зміни методики збору.

## Аудит методики (що перевірено і де діри)
- **Слоти не переоптимізовані.** `deriveSlots` жадібно підбирає слоти на тих самих
  рядках, якими потім міряє `slotsStats` — класичний in-sample. Крос-валідація по
  днях (2 фолди, підбір на одній половині днів, вимір на іншій) дає **290.5 ₴/год**
  проти 294 in-sample. Оптимізм **лише +3.5 ₴/год** — метод здоровий, окремої
  поправки в звіті не потребує. Перевіряти повторно при зміні `deriveSlots`.
- **Утилізація 89.7%** (57.7 год модельних циклів на 64.3 год змін) — модель
  часу сходиться з реальністю. Для порівняння, стара `time_model` давала 118%,
  тобто фізично неможливе значення.
- **Реальність зміни** (14 змін): медіана ≈ **11.7 поїздок, 4.6 год, 1137 ₴**.
  Довгі зміни дають гіршу ставку (≥8 год → 231 ₴/год, <8 год → 268; кореляція
  тривалості з ₴/год **−0.48**), але це **3 зміни проти 9** — сигнал слабкий.
  Всередині зміни втоми не видно: перша третина 268 → остання 294 ₴/год, тобто
  падіння дає не втома, а те, що довгі зміни випадають на слабкий попит.
- **Година виїзду — слабкий важіль.** Профіль рівний (222–312 ₴/год), вираженого
  вікна немає; найгірше 11:00 (222), найкраще 21:00 (312), але n малі. Надбавка
  за годинами теж не має стабільного піку. Планувати графік за цим не варто.
- **Концентрація помірна**: топ 20% поїздок дають 55% надлишку над ціллю, топ
  50% — 89%. Тобто середнє не тримається на кількох викидах.
- ⚠️ **Діра: `max_pickup_km` не бектеститься.** `pickup_km` заданий у **0 із 164**
  поїздок, тому `slotPass`/`modePass` пропускають перевірку подачі повністю.
  Радіус подачі (1.5 / 3 / 3 км у слотах) — **єдине поле фільтра, яке ми ставимо
  навмання**: ми не знаємо ні скільки замовлень втратимо звуженням, ні скільки
  часу зекономимо. Пальне подачі при цьому враховане (порожняк 0.3 × середні
  5.6 км ≈ 1.7 км уже закладено), і час теж — `cycle_model` виміряна з інтервалів
  між стартами, тож подача в ній є. Додавати подачу до часу/газу окремо —
  **подвійний рахунок**. Щоб важіль став керованим, треба збирати `pickup_km`.

## Подача (`pickup_km`) — семантика, ціна, збір

**Що таке «Звідки → Відстань» у фільтрі** (підтверджено): Uklon рахує
**відстань ма��шруту** від поточної позиції водія до точки, де забирати клієнта.
Тобто це справжня подача, яка рухається за тобою, а не коло навколо
зафіксованої точки. Отже вся економіка нижче застосовна прямо.

**Ціна подачі** — `lib.ts::pickupCostPerKm`:
```
pickupCostPerKm = fuelPerKm + target_net_per_hour × cycle_model.per_km_min / 60
```
Наразі ≈ **13.05 ₴/км** (пальне 5.89 + час 7.16).

**Нуль відліку** — `lib.ts::typicalPickupKm`: типова подача **вже врахована**
(порожняк 0.3 × середні 5.9 км ≈ **1.8 км** у пальному; час — у виміряному циклі).
Тому питання завжди «скільки км ПОНАД типову», а не «з нуля». Коли назбирається
≥20 фактичних `pickup_km`, функція автоматично перейде на факт.

**Радіус заробляється ціною** — `lib.ts::earnedPickupKm`: беремо 10-й перцентиль
надлишку над ціллю серед замовлень, які слот пропускає **за ціною**
(`slotPassPrice`, без перевірки подачі — інакше колове визначення), ділимо на
ціну км. Радіус витримують ~90% потоку слота.

| ₴/км | заслужений радіус (90% потоку) |
|---|---|
| 20 | **2.0 км** |
| 28 (К=5) | 2.5 км |
| 32 (К=5) | 3.5 км |

> Практичний висновок: **радіус не можна призначати окремо від ціни.** Раніше
> слоти мали захардкоджені 1.5/3/3 км — це була вигадка. На 20 ₴/км усі три
> заробляють лише 2 км.

**Пастка АБО.** Радіус слота обмежує лише ті замовлення, які приймає **тільки
він один**. Зараз 143 із 164 беруть 2+ слоти (ексклюзивних — 4), бо `deriveSlots`
видає майже однакові слоти (всі 20 ₴/км, різниця лише в «км у мінімалці»).
Тому жорсткий радіус на одному слоті нічого не ріже — ширший слот пропустить.
Щоб драбина запрацювала, ціна й радіус мають рости **разом**:
дешевий слот → тільки впритул, далека подача → тільки за високий поріг.
У 4-режимній моделі проблеми немає: там активний один режим (виходить І, не АБО).

**Ручна політика радіуса (до налаштування фільтрів)**: у глухому куті ставив
**ширше** (до 4 км), у живій зоні **2 км** (3 у затишшя). Логіка зворотна до
«ціни км»: у тупику мало замовлень, тож вузький радіус = простій. Тобто радіус
балансує не лише вартість, а й **доступність** замовлень — цього боку наша
модель поки не бачить.

> ⚠️ **Поправка до висновку про тупики.** Результат «після тупика хвіст лише
> +2.6 хв» отримано на даних, де водій **компенсував** тупики ширшим радіусом
> (до 4 км). Ця зайва подача сидить усередині виміряного інтервалу, тож висновок
> «тупики не дорожчі за часом» справедливий **за умови такої компенсації**, а не
> безумовно. Частину ціни тупика сплачено кілометрами подачі, яких ми не бачимо.
> Розділити одне від одного можна буде лише зібравши `pickup_km`.

**Збір**: поля `pickup_km` і `pickup_min` є в `types.ts::Trip` — записуються
вручну з екрана пропозиції (Uklon показує і відстань, і час підльоту, напр. «7 хв»).
`slotPass`/`modePass` почнуть перевіряти подачу автоматично, щойно `pickup_km`
з'явиться (поки `null` — перевірка пропускається). Достатньо **20–30 замірів**,
щоб `typicalPickupKm` перейшла на факт і радіус перестав бути здогадкою.
Пара «км + хв» додатково розділяє «далеко» і «пробка» та калібрує темп руху
напряму, без регресії.

```json
{ "datetime": "22.08 18:40", "payment": "Готівка", "amount": 150,
  "distance": 4.2, "pickup_km": 1.8, "pickup_min": 7,
  "from": "...", "to": "...", "zone": "Місто" }
```

### Простої (`data.idles`) — ціна відмови
Останнє, чого модель не бачить: скільки коштує **звуження** фільтра. Відхилені
фільтром замовлення нам не видно в принципі, тому єдине джерело — ручний запис
простоїв (`types.ts::Idle`, масив `data.idles`):

```json
{ "datetime": "22.08 19:15", "minutes": 25, "zone": "Глухий кут", "radius_km": 2 }
```

Навіщо кожне поле:
- `minutes` + `datetime` — коли і скільки стояв (година доби важлива: попит нерівний).
- `zone` — **закриває питання про тупики**, яке на самих поїздках не вирішується:
  зараз ми бачимо лише інтервали між прийнятими замовленнями, а не те, скільки
  часу простояв, не отримавши нічого.
- `radius_km` — який радіус стояв у фільтрі під час простою. Це **єдиний спосіб**
  виміряти, скільки простою купує вузький радіус, і перевести вибір «2 чи 3 км»
  з інтуїції в розрахунок.

## Конвенції
- Мова спілкування й коментарів — **українська**.
- Числа: відстань `км` з 2 знаками, суми `грн`.
- Дата/час у `datetime`: `DD.MM HH:MM` (місяць — серпень `.08`).
- Не коммітити `report.html`, `*.xlsx`, `node_modules/`, `.DS_Store`, `.idea/` (див. `.gitignore`).
- Скрипти мають лишатися ідемпотентними: `generate`/`update-pay` можна запускати повторно.
- Формули живуть **в одному місці** — `src/lib.ts`. `generate/analyze/report` імпортують `compute` звідти.
- **Не запускати великі скрипти через `node -e` в терміналі.** Багаторядкову логіку
  писати у виконуваний файл (`.ts`/`.mjs`) і запускати його. Одноразові аналізи —
  у `/tmp/*.mjs` (поза репо); постійні інструменти — у `src/`. Термінал лишати для
  коротких однорядкових команд.

## Обмеження
- OCR/детекція оплати — лише **macOS** (Apple Vision + jimp).
- `src/updatePay.ts` зіставляє скрін↔поїздку за **відбитком «сума + дистанція»**
  (не за датою!): свіжі замовлення застосунок показує **без дати**, лише з часом.
  Пара сума+км практично унікальна (163/164 на базі); рідкісні колізії розрізняються
  часом `HH:MM` зі скріна (перші 3 рядки пропускаються — там годинник статус-бара).
  Поїздки все одно треба додати в `data.json` заздалегідь (`npm run parse`).

